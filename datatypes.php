<?php
/*
Integer, String, float (only float here), boolean, array, null are the data types
*/
$a=10;
$b="hello";
$c=12.5434;
$d= true;
$e= array(1,4, "hello", true, null); //indexed array
// $arr= array("key"=>"value",); //associative array
$f=null;

// html forms submit in key val pair, if field left empty, it is stored as null

echo (var_dump($e));

?>