<?php
echo "hello";

// single comment
#  single

/*
multi line
*/

// index is default page that local host opens, after directing to htdocs in xampp
//else if one page, takes that; if multiple, shows as directory in browser

// php is not case sensitive, but variable name is
//php is server side language, works in browser

// variable with $
// a-z A-Z 0-9 and underscore _ , but can start with only letter or underscore, no number, after dollar

$a =5;
$b= "Hello World";

echo $a;
echo(var_dump($a)."<br>");
echo(var_dump($b));

//var_dump gives type of variable, for int it gives type and value, for string it gives type length and value

?>

